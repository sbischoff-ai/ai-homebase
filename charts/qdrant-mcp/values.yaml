nameOverride: ""
fullnameOverride: ""

replicaCount: 1

image:
  repository: python
  tag: "3.12-slim"
  pullPolicy: IfNotPresent

imagePullSecrets: []

global:
  imagePullSecrets: []
  hosts:
    qdrantMcp: ""
  commonLabels: {}

service:
  type: ClusterIP
  port: 8000
  targetPort: 8000
  annotations: {}

ingress:
  enabled: true
  className: ""
  annotations: {}
  defaultHost: qdrant-mcp.example.com
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []

qdrant:
  url: http://platform-stack-qdrant:6333
  collectionName: openclaw-memory

embedding:
  provider: fastembed
  model: BAAI/bge-base-en-v1.5

toolDescriptions:
  store: |
    Store a memory for cross-agent recall. Use this to preserve durable knowledge such as user preferences, decisions, conventions, patterns, incidents, plans, creative content, and reusable context.

    REQUIRED: Store one atomic durable claim per memory. The `information` parameter must begin with domain and kind tags in brackets, followed by 1 to 3 compact, self-contained, retrieval-optimized sentences:
      [real|speculative|fictional|synthetic] [user-preference|user-context|decision|convention|pattern|fact|plan|task-context|incident|monitor-rule|creative|relationship|reference] Your memory text here.

    Include natural retrieval anchors in `information` when relevant: project slug, service/component, agent/person, artifact path, common aliases, incident name, and source reference. Metadata alone does not improve vector ranking, so important recall terms must appear in `information`.

    REQUIRED: The `metadata` parameter must be a JSON object with at minimum:
      {"kind": "...", "domain": "...", "agent": "...", "created": "ISO-8601"}

    Optional metadata: confidence (high|medium|low), project, tags (array), nc_refs (array of {type, id, path, label}), supersedes, expiry, source_url.

    Guidelines:
    - Store facts you would want any agent to recall weeks later.
    - Split unrelated decisions, facts, preferences, incidents, or document summaries into separate memories.
    - Do not store multi-topic summaries as one memory.
    - Do not store ephemeral task status, transient tool output, or easily re-derivable information.
    - Do not store secrets or credentials.
    - For fictional or creative content, always use domain `fictional`.
    - If correcting a prior memory, store a new append-only replacement memory with `supersedes` in metadata and mention the correction in the text.
    - The current Qdrant MCP tools cannot update, delete, merge, or mark existing entries.
    - Include Nextcloud references in both the text and `nc_refs` metadata when relevant.
  find: |
    Search shared semantic memory across all agents. Returns memories ranked by relevance.

    Use this tool before answering questions about user preferences, prior decisions, established conventions, past incidents, project history, people and relationships, creative worlds, or anything that may have been stored previously.

    Query tips:
    - Include a domain tag when useful: `[real] user's preferred editor` or `[fictional] Ashenmoor political structure`
    - Include a kind tag when useful: `[decision] database choice for Nextcloud`
    - Use natural language for broad recall.
    - Be specific when possible.
    - If results mix real and fictional content, re-query with an explicit `[real]` or `[fictional]` prefix.

    Important:
    - This searches all memories from all agents.
    - Returned memories include metadata such as kind, domain, agent, confidence, and Nextcloud references.
    - Treat speculative or low-confidence results cautiously.
    - Treat expired memories as potentially stale.

    Filtering:
    - You can filter by metadata fields using the `query_filter` parameter.
    - Qdrant MCP stores memory metadata under nested payload keys. Use `metadata.created`, `metadata.kind`, `metadata.domain`, `metadata.agent`, `metadata.confidence`, `metadata.project`, and `metadata.tags`.
    - Combine semantic search terms with metadata filters for project, domain, kind, agent, and recency.
    - Example: Find memories from the last 24 hours:
      query_filter: {"must": [{"key": "metadata.created", "range": {"gte": "2026-04-02T00:00:00Z"}}]}
    - Example: Find memories by a specific agent:
      query_filter: {"must": [{"key": "metadata.agent", "match": {"value": "coder"}}]}
    - Example: Find decisions from a specific project:
      query_filter: {"must": [{"key": "metadata.domain", "match": {"value": "real"}}, {"key": "metadata.kind", "match": {"value": "decision"}}, {"key": "metadata.project", "match": {"value": "ai-homebase"}}]}
    - Example: Find memories with a tag:
      query_filter: {"must": [{"key": "metadata.tags", "match": {"value": "qdrant"}}]}
    - Combine semantic search (the `query` parameter) with filters for best results.

command:
  - /bin/sh
  - -ceu
  - |
    if [ ! -x /tmp/qdrant-mcp/bin/mcp-server-qdrant ]; then
      python -m venv /tmp/qdrant-mcp
      /tmp/qdrant-mcp/bin/pip install --no-cache-dir "mcp-server-qdrant==0.8.0"
    fi

    startup_log=/tmp/qdrant-mcp-startup.log
    if /tmp/qdrant-mcp/bin/mcp-server-qdrant --transport streamable-http 2>"${startup_log}"; then
      exit 0
    fi

    startup_status=$?
    if grep -q "model_optimized.onnx failed:Load model .* File doesn't exist" "${startup_log}"; then
      echo "Detected incomplete FastEmbed cache in /tmp/fastembed_cache; clearing cache and retrying once." >&2
      rm -rf /tmp/fastembed_cache
      exec /tmp/qdrant-mcp/bin/mcp-server-qdrant --transport streamable-http
    fi

    cat "${startup_log}" >&2
    exit "${startup_status}"

podAnnotations: {}
podLabels: {}

env:
  - name: HOME
    value: /tmp
  - name: XDG_CACHE_HOME
    value: /tmp/.cache
  - name: HF_HOME
    value: /tmp/.cache/huggingface
  - name: HUGGINGFACE_HUB_CACHE
    value: /tmp/.cache/huggingface/hub
  - name: FASTMCP_HOST
    value: "0.0.0.0"
  - name: QDRANT_ALLOW_ARBITRARY_FILTER
    value: "true"
envFromSecrets: []
existingSecret: ""
secretRefs: []

podSecurityContext:
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  capabilities:
    drop:
      - ALL

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

probes:
  startup:
    enabled: true
    type: tcp
    periodSeconds: 10
    failureThreshold: 180
  readiness:
    enabled: true
    type: tcp
    initialDelaySeconds: 180
    periodSeconds: 10
  liveness:
    enabled: true
    type: tcp
    initialDelaySeconds: 300
    periodSeconds: 15

nodeSelector: {}
tolerations: []
affinity: {}
