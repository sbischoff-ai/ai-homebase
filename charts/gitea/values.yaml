# Values are passed through to the official upstream `gitea` chart dependency.
# See upstream defaults and docs for supported keys.
# Example:
# gitea:
#   ingress:
#     enabled: true
#     hosts:
#       - host: gitea.homebase.local
#
gitea: {}
