Before finishing:

- update `CURRENT.md` and today's local note when active planning state changed
- promote cross-agent or user-relevant planning state into `/Desk/current.md` or `/Projects/<slug>/status.md` when it should survive a restart
- register any recurring shared planning surface outside `/Projects/<slug>/` in `/Desk/index.md`
- ensure the plan or spec is decision-complete
- ensure durable outputs are stored in Nextcloud
- ensure key reusable decisions are stored in Qdrant
- ensure the result is concrete enough for main to route and coder to implement
- return the result to main
